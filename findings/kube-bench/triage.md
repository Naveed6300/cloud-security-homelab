| Finding                                                         | Severity | Image                                             | Reachable?                                                                                                                   | Action                                                                                                                                                                                         |
| --------------------------------------------------------------- | -------: | ------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **KSV-0041: ClusterRole/admin can manage secrets**              | Critical | N/A, RBAC misconfiguration on `ClusterRole/admin` | **Conditional:** reachable if any user, group, or service account is bound to `admin`                                        | Do **not** bind broad users/SAs to `admin`; create a least-privilege Role/ClusterRole without `secrets` access, or restrict secret access to tightly scoped break-glass/admin identities only. |
| **KSV-0053: ClusterRole/admin can exec into pods**              |     High | N/A, RBAC misconfiguration on `ClusterRole/admin` | **Conditional:** reachable if a bound identity can use `pods/exec`                                                           | Remove or avoid granting `pods/exec` write access for routine admin roles; reserve exec access for controlled break-glass troubleshooting with audit logging.                                  |
| **KSV-0056: ClusterRole/admin can manage networking resources** |     High | N/A, RBAC misconfiguration on `ClusterRole/admin` | **Conditional:** reachable if a bound identity can modify Services, Endpoints, EndpointSlices, NetworkPolicies, or Ingresses | Use least-privilege networking roles; allow routine users read-only access to networking resources and restrict create/update/delete to trusted platform/network admins.                       |

