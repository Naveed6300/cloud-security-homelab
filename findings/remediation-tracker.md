# Remediation Tracker

**Cluster:** k3s homelab (k3s-cp01, k3s-agent01, k3s-agent02)  
**Scan date:** 2026-05-25  
**Tools:** kube-bench v0.9.4, Kubescape v4.0.8, Trivy, Falco 0.43.1

---

| ID | Source | Control | Finding | Severity | Notes + Solution | Status |
|---|---|---|---|---|---|---|
| F-001 | Kubescape NSA / ArmoBest — C-0034 | Automatic mapping of service account | Default ServiceAccounts across all namespaces auto-mount API tokens. Any pod that doesn't explicitly set a SA gets a credential mounted, expanding the blast radius of a compromised pod. | Medium | **Solution:** `kubectl patch serviceaccount default -n <ns> -p '{"automountServiceAccountToken":false}'` applied to: storage, security, gatekeeper-system, longhorn-system, default. **Residual:** Named SAs (non-default) still require individual patching. | ✅ Remediated |
| F-002 | Kubescape NSA / ArmoBest — C-0012 | Application credentials in configuration files | Traefik helm install Jobs expose a credential in `spec.template.spec.containers[0].env[8]`. The Jobs are ephemeral (completed state) so live exposure is low, but the pattern indicates helm install credentials are not secrets-managed. | High | **Solution:** Inspect the env var (`kubectl get job helm-install-traefik -n kube-system -o yaml \| grep -A3 env`). If it's a registry token or kubeconfig, rotate it and use a Secret reference instead of a literal value in helm args. | 🔄 Pending |
| F-003 | Kubescape NSA / ArmoBest — C-0057 | Privileged container — Falco | Falco DaemonSet runs as privileged (`securityContext.privileged: true`). | High | **Accepted:** Falco requires privileged to load the modern_ebpf probe and attach to kernel syscalls. Mitigations in place: (1) `security` namespace is scoped by Gatekeeper constraints, (2) the privilege is explicitly documented and audited in phase 04. Production pattern: use the Kubescape node-agent + OPA constraints to audit-but-not-deny privileged on known-good workloads. | 🚫 Will Not Fix |
| F-004 | Kubescape NSA / ArmoBest — C-0057 | Privileged container — Longhorn | longhorn-manager, engine-image, instance-manager, and longhorn-csi-plugin all run as privileged. | High | **Accepted:** Longhorn requires privileged for block device access, CSI operations, and iSCSI management on bare metal. This is a fundamental constraint of distributed storage on Linux — not fixable without replacing the storage subsystem. | 🚫 Will Not Fix |
| F-005 | Kubescape NSA — C-0046 | Insecure capabilities | longhorn-csi-plugin adds `SYS_ADMIN` capability to its containers. | High | **Accepted:** `SYS_ADMIN` is required for mount operations in the CSI plugin. Longhorn upstream has not removed this requirement. In production, scope with a stricter PodSecurityPolicy or OPA exception that allows SYS_ADMIN only for `longhorn-system`. | 🚫 Will Not Fix |
| F-006 | Kubescape ArmoBest — C-0237 | Image signatures not verified | 24 of 26 workloads pull unsigned images (8% compliance). No cosign/sigstore verification in the admission pipeline. | High | **Lab scope:** Cosign requires a signing infrastructure (keys, CI signing step, admission policy via Gatekeeper/Kyverno). This is documented as a future phase. The phase 04 registry allowlist mitigates by restricting pull sources to audited registries. **Next step:** Add `ghcr.io/sigstore/cosign` to the toolchain and write a Gatekeeper constraint to enforce signed images for the storage namespace. | 🔄 Pending |
| F-007 | Kubescape NSA — C-0270 / C-0271 | CPU and memory limits not set | 18 resources missing CPU limits, 16 missing memory limits. Affected: Traefik, Gatekeeper, Longhorn CSI components, Falco falcoctl sidecar, nextcloud-cron. | High | **Partial fix available:** The phase 04 Gatekeeper `pods-require-resource-limits` constraint enforces limits on the `storage` and `default` namespaces. System namespaces (kube-system, longhorn-system, security) are exempt. **Solution for system namespaces:** Add resource limits in each component's helm values. Traefik and Gatekeeper accept `resources.limits` in their chart values. Longhorn limits can be set via the Longhorn UI. | 🔄 Pending |
| F-008 | Kubescape NSA — C-0016 | Allow privilege escalation | 19 containers missing `allowPrivilegeEscalation: false` and `privileged: false` in their securityContext. Includes nextcloud, minio, postgres, and system components. | Medium | **Solution for our workloads:** Add to helm values for nextcloud, minio: `securityContext.allowPrivilegeEscalation: false`. Postgres: same via `podSecurityContext`. System components require upstream chart changes. | 🔄 Pending |
| F-009 | Kubescape NSA — C-0013 | Non-root containers | 20 containers not configured with `runAsNonRoot: true`. | Medium | **Partial:** Postgres must run as uid 999 (postgres user) — `runAsNonRoot` is achievable with `runAsUser: 999`. Nextcloud runs as www-data (uid 33) — same. Longhorn components require root for block device ops. **Solution:** Add `securityContext.runAsNonRoot: true` and `runAsUser` to nextcloud and minio helm values. | 🔄 Pending |
| F-010 | Kubescape NSA — C-0030 | Ingress and Egress not blocked | 21 resources in kube-system, longhorn-system, gatekeeper-system, and security namespaces have no NetworkPolicy. | Medium | **Accepted for lab:** Phase 06 applied default-deny + explicit allows to the `storage` namespace. System namespaces require careful policy design — misconfigured NetworkPolicy on kube-system or longhorn-system can break the cluster. This is production hardening work beyond the lab's scope. | 🚫 Will Not Fix |
| F-011 | Kubescape NSA — C-0055 | Linux hardening (seccomp/capabilities drop) | 19 containers missing RuntimeDefault seccomp profile, SELinux options, and `capabilities.drop: [ALL]`. | Medium | **Solution:** Add to securityContext for storage namespace workloads: `seccompProfile: {type: RuntimeDefault}` and `capabilities: {drop: [ALL]}`. This is additive and low-risk for applications that don't need specific capabilities. | 🔄 Pending |
| F-012 | Kubescape NSA | Secret/etcd encryption at rest | etcd data not encrypted at rest. Kubernetes Secrets are stored in plaintext in the etcd datastore. | Medium | **Infrastructure limitation:** Requires `--secrets-encryption` flag on k3s server and a key rotation procedure. Valid production requirement. **Solution:** Add `secrets-encryption: true` to `/etc/rancher/k3s/config.yaml` and restart k3s. Run `k3s secrets-encrypt rotate-keys` after. | 🔄 Pending |
| F-013 | Kubescape NSA | Audit logs not enabled | Kubernetes API audit logging is not configured. No record of API calls for forensic purposes. | Medium | **Infrastructure limitation:** Requires adding `--kube-apiserver-arg=audit-log-path=/var/log/k8s-audit.log` and an audit policy file to the k3s server config. **Solution:** Create `/etc/rancher/k3s/audit-policy.yaml`, reference it in `/etc/rancher/k3s/config.yaml`, restart k3s. | 🔄 Pending |
| F-014 | kube-bench 4.2.1–4.2.9 | Kubelet configuration — anonymous auth, authorization mode, TLS | 5 CIS k3s-cis-1.7 checks FAIL for kubelet settings. | High | **k3s false positive.** kube-bench checks kubeadm-style config paths (`/var/lib/kubelet/config.yaml`, process flags) that don't exist in k3s. Verified: (1) no matching kubelet flags in process list — k3s configures kubelet internally as a goroutine, (2) port 10255 (read-only) is confirmed closed. k3s applies secure defaults that kube-bench cannot verify via its standard checks. | 🚫 Will Not Fix |
| F-015 | Kubescape NSA — C-0017 | Immutable container filesystem | 19 containers not using `readOnlyRootFilesystem: true`. | Low | **Complex for stateful workloads:** Postgres, Nextcloud, and MinIO write to their filesystems at runtime. Making root read-only requires explicit writable volume mounts for each path that needs writes (`/tmp`, data dirs, log dirs). Achievable but requires per-workload analysis. | 🔄 Pending |
| F-016 | Falco runtime | Terminal shell in container — T1059 | Shell spawned in `shellme` test pod (security namespace) via `kubectl exec`. Default Falco rule fired at NOTICE. | Notice | **Expected detection — lab-generated.** Rule: "Terminal shell in container". Maps to MITRE T1059 (Command and Scripting Interpreter). In production: alert should page on-call for any shell spawned in the storage namespace. | ✅ Detection confirmed |
| F-017 | Falco runtime | Read sensitive file untrusted — T1555 | `cat /etc/shadow` executed inside test pod triggered "Read sensitive file untrusted" at WARNING. | Warning | **Expected detection — lab-generated.** Maps to MITRE T1555 (Credentials from Password Stores) / mitre_credential_access. Evidence saved to `falco/falco-shell-alert.json`. | ✅ Detection confirmed |
| F-018 | Falco runtime | Package manager run in container — T1072 | `apk add curl` inside test pod triggered custom rule at WARNING. | Warning | **Expected detection — lab-generated.** Custom rule written for this lab. Maps to MITRE T1072 (Software Deployment Tools) / mitre_lateral_movement. Demonstrates custom ATT&CK-mapped detection. | ✅ Detection confirmed |
| F-019 | Falco runtime | Drop and execute new binary in container | `curl` binary downloaded and executed in test pod triggered CRITICAL default rule. | Critical | **Expected detection — lab-generated.** "Drop and execute new binary in container" fires when a binary is executed from a path not in the original container image layer. High-signal rule for post-exploitation tool staging. | ✅ Detection confirmed |
| F-020 | Trivy | Image vulnerabilities — HIGH/CRITICAL | Trivy cluster scan found HIGH and CRITICAL CVEs across cluster images. Full output in `trivy/trivy-k8s-summary.txt`. | High/Critical | **Ongoing:** Trivy scan covers all running images. Triage by exposure and reachability — severity alone is insufficient for prioritization. Images in the `storage` namespace (nextcloud, postgres, minio) should be pinned to versions with active CVE remediation. | 🔄 Pending |

---

## Status Key

| Symbol | Meaning |
|---|---|
| ✅ Remediated | Fix applied and verified |
| ✅ Detection confirmed | Falco rule fired as expected (positive outcome) |
| 🔄 Pending | Real risk, fix identified, not yet applied |
| 🚫 Will Not Fix | Accepted risk (documented rationale) or infrastructure limitation |
